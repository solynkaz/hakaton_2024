{
  "browserSettings": {
    "favicon": "favicon.ico",
    "title": "СМА"
  },
  "keycloak": {
    "url": "${KEYCLOAK_URL}",
    "realm": "${KEYCLOAK_REALM}",
    "clientId": "${KEYCLOAK_CLIENT_ID}"
  },
  "keycloakInitOptions": {
    "checkLoginIframe": false
  }
}
